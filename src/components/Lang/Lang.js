import React from 'react';
import './lang.css';

const Lang = () => {
  return (
    <select className='select'>
        <option className='opt' value="UK">UK</option>
        <option className='opt' value="UA">UA</option>
    </select>
  )
}

export default Lang
